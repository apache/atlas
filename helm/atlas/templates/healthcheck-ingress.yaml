{{- if .Values.atlas.healthcheckIngress.enabled -}}
{{- $ingressApiIsStable := eq (include "atlas.ingress.isStable" .) "true" -}}
{{- $ingressSupportsIngressClassName := eq (include "atlas.ingress.supportsIngressClassName" .) "true" -}}
{{- $ingressSupportsPathType := eq (include "atlas.ingress.supportsPathType" .) "true" -}}
{{- $servicePort := .Values.atlas.service.port -}}
{{- $ingressPath := .Values.atlas.healthcheckIngress.path -}}
{{- $ingressPathType := .Values.atlas.healthcheckIngress.pathType -}}
{{- $extraPaths := .Values.atlas.healthcheckIngress.extraPaths -}}
apiVersion: {{ include "atlas.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: atlas-healthcheck-atlas
  namespace: {{ .Values.Namespace  }}
  labels:
    app: {{ template "name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  {{- if .Values.atlas.healthcheckIngress.annotations }}
  annotations:
    {{- range $key, $value := .Values.atlas.healthcheckIngress.annotations }}
      {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
spec:
  {{- if and $ingressSupportsIngressClassName .Values.atlas.secondaryIngress.ingressClassName }}
  ingressClassName: {{ .Values.atlas.secondaryIngress.ingressClassName }}
  {{- end -}}
{{- if .Values.atlas.secondaryIngress.tls }}
  tls:
{{ tpl (toYaml .Values.atlas.secondaryIngress.tls) $ | indent 4 }}
{{- end }}
  rules:
  {{- if .Values.atlas.secondaryIngress.hosts  }}
  {{- range .Values.atlas.secondaryIngress.hosts }}
    - host: {{ tpl . $}}
      http:
        paths:
{{- if $extraPaths }}
{{ toYaml $extraPaths | indent 10 }}
{{- end }}
          - path: {{ $ingressPath }}
            {{- if $ingressSupportsPathType }}
            pathType: {{ $ingressPathType }}
            {{- end }}
            backend:
              {{- if $ingressApiIsStable }}
              service:
                name: atlas-ui-service
                port:
                  number: {{ $servicePort }}
              {{- else }}
              serviceName: atlas-ui-service
              servicePort: {{ $servicePort }}
              {{- end }}
  {{- end }}
  {{- else }}
    - http:
        paths:
          - backend:
              {{- if $ingressApiIsStable }}
              service:
                name: atlas-ui-service
                port:
                  number: {{ $servicePort }}
              {{- else }}
              serviceName: atlas-ui-service
              servicePort: {{ $servicePort }}
              {{- end }}
            {{- if $ingressPath }}
            path: {{ $ingressPath }}
            {{- end }}
            {{- if $ingressSupportsPathType }}
            pathType: {{ $ingressPathType }}
            {{- end }}
  {{- end -}}
{{- end }}